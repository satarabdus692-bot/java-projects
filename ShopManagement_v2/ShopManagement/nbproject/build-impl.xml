<?xml version="1.0" encoding="UTF-8"?>
<!--
*** GENERATED FROM project.xml - DO NOT EDIT  ***
***         EDIT ../build.xml INSTEAD         ***
-->
<project name="ShopManagement-impl" basedir=".." xmlns:j2seproject1="http://www.netbeans.org/ns/j2se-project/1" xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3" xmlns:jaxrpc="http://www.netbeans.org/ns/j2se-project/jax-rpc">
    <fail message="Please build using Ant 1.7.1 or higher.">
        <condition>
            <not>
                <antversion atleast="1.7.1"/>
            </not>
        </condition>
    </fail>
    <property file="nbproject/private/private.properties"/>
    <property file="${user.home}/.netbeans/7.0/build.properties"/>
    <property file="nbproject/project.properties"/>

    <condition property="dist.jar.stripped" value="${dist.dir}/ShopManagement.jar">
        <isset property="dist.jar"/>
    </condition>

    <!-- Helper properties -->
    <property name="platform.home"    value="${default_platform.home}"/>
    <property name="platform.bootcp"  value="${boot.classpath}"/>
    <property name="run.jvmargs"      value=""/>
    <property name="run.jvmargs.ide"  value=""/>
    <property name="javac.compilerargs" value=""/>
    <property name="work.dir"         value="${basedir}"/>
    <condition property="no.deps">
        <and><istrue value="${no.dependencies}"/></and>
    </condition>

    <!-- ==================== INIT ==================== -->
    <target name="-pre-init"/>
    <target name="-post-init"/>
    <target name="-init-private" depends="-pre-init">
        <property file="nbproject/private/private.properties"/>
    </target>
    <target name="-init-user" depends="-init-private">
        <property file="${user.home}/.netbeans/7.0/build.properties"/>
    </target>
    <target name="-init-project" depends="-init-user">
        <property file="nbproject/project.properties"/>
    </target>
    <target name="-do-init" depends="-init-project">
        <property name="platform.java"  value="${java.home}/bin/java"/>
        <property name="platform.javac" value="${java.home}/bin/javac"/>
        <property name="platform.jar"   value="${java.home}/bin/jar"/>
        <available file="${java.home}/../bin/javac"
                   property="platform.javac"
                   value="${java.home}/../bin/javac"/>
    </target>
    <target name="-post-do-init"/>
    <target name="-init-check" depends="-do-init,-post-do-init">
        <fail unless="src.dir">Must set src.dir</fail>
        <fail unless="test.src.dir">Must set test.src.dir</fail>
        <fail unless="build.dir">Must set build.dir</fail>
        <fail unless="dist.dir">Must set dist.dir</fail>
        <fail unless="build.classes.dir">Must set build.classes.dir</fail>
        <fail unless="dist.javadoc.dir">Must set dist.javadoc.dir</fail>
        <fail unless="build.test.classes.dir">Must set build.test.classes.dir</fail>
        <fail unless="build.test.results.dir">Must set build.test.results.dir</fail>
        <fail unless="build.classes.excludes">Must set build.classes.excludes</fail>
        <fail unless="dist.jar">Must set dist.jar</fail>
    </target>
    <target name="init" depends="-pre-init,-init-private,-init-user,-init-project,-do-init,-post-do-init,-init-check"/>

    <!-- ==================== COMPILE ==================== -->
    <target name="-pre-compile"/>
    <target name="-post-compile"/>
    <target name="compile" depends="init,-pre-compile">
        <mkdir dir="${build.classes.dir}"/>
        <javac srcdir="${src.dir}"
               destdir="${build.classes.dir}"
               classpath="${javac.classpath}"
               includeantruntime="false"
               encoding="${source.encoding}"
               source="${javac.source}"
               target="${javac.target}"
               debug="true"
               debuglevel="lines,vars,source">

        </javac>
        <copy todir="${build.classes.dir}">
            <fileset dir="${src.dir}" excludes="${build.classes.excludes},${excludes}" includes="${includes}"/>
        </copy>
    </target>

    <!-- ==================== JAR ==================== -->
    <target name="-pre-jar"/>
    <target name="-post-jar"/>
    <target name="jar" depends="compile,-pre-jar">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.jar}" basedir="${build.classes.dir}" manifest="${basedir}/manifest.mf">
            <manifest>
                <attribute name="Main-Class" value="${main.class}"/>
                <attribute name="Class-Path" value="${run.classpath.with.dist.jar}"/>
            </manifest>
        </jar>
    </target>

    <!-- ==================== CLEAN ==================== -->
    <target name="-pre-clean"/>
    <target name="-post-clean"/>
    <target name="clean" depends="init,-pre-clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <!-- ==================== RUN ==================== -->
    <target name="-pre-run-single"/>
    <target name="-do-run">
        <java classname="${main.class}"
              classpath="${run.classpath}"
              fork="true"
              failonerror="true"
              dir="${work.dir}">
            <jvmarg value="-Xmx256m"/>
            <jvmarg line="${run.jvmargs}"/>
        </java>
    </target>
    <target name="-post-run"/>
    <target name="run" depends="compile,-pre-run-single,-do-run,-post-run"
            description="Run project."/>

    <!-- ==================== DEFAULT ==================== -->
    <target name="default" depends="run" description="Build and run project."/>

    <!-- ==================== TEST (stub) ==================== -->
    <target name="test" depends="compile" description="Run tests (none defined)."/>
    <target name="test-single" depends="compile" description="Run single test."/>
    <target name="javadoc" depends="init" description="Build Javadoc.">
        <mkdir dir="${dist.javadoc.dir}"/>
        <javadoc sourcepath="${src.dir}" destdir="${dist.javadoc.dir}" encoding="${source.encoding}"/>
    </target>
    <target name="profile" depends="run"/>
    <target name="build-tests" depends="compile"/>
    <target name="debug" depends="compile"/>
    <target name="debug-test" depends="compile"/>
    <target name="debug-fix" depends="compile"/>
</project>
